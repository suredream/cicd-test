Here’s a memo-style summary email you can send.

---

**Subject:** Summary & follow-ups: ACH model validation, testing, and foundational work

Hi all,

As a follow‑up to our recent discussion on **ACH model migration and validation**, I wanted to summarize the key points and potential follow‑up areas as a memo for us to explore further.

---

### 1. Validation flow & key dates

- **Feature validation before model validation**  
  We agreed DS should first validate the **feature outputs** (on prod data) before validating model outputs, to isolate issues between feature pipelines and the model itself.

- **Dark launch and full release timeline**  
  There is a planned **dark mode release** (e.g., around 10/2) followed by a **full release** (e.g., around 10/9), with DS using the dark-mode window to validate model outputs.

- **Feature validation window (late September)**  
  Tentative plan is to have **inmo validate features first**, then DS validate features between roughly **9/24–9/29 or 9/30**, giving them a few days for QA and sign‑off.

- **Data scientists’ UAT sign‑off as true UAT**  
  The prior “UAT” using non‑real data is considered “fake UAT”; the **actual UAT** is DS validating on real production-like data and providing formal sign‑off.

---

### 2. Scope split: what is on DS vs engineering

- **DS validation of features and model outputs**  
  DS own the **approach and criteria** for validating features and model outputs; engineering focuses on providing correct data and tooling, not on deciding statistical checks.

- **Engineering validation between OSDS and GCP**  
  Engineering will validate that **GCP outputs match OSDS** (e.g., via distribution comparisons) to ensure that code migration preserves behavior.

- **Model monitoring vs pipeline monitoring**  
  **Model monitoring** (drift, re‑training decisions) belongs to a dedicated team, while engineering focuses on **pipeline health** (runs, volumes, performance).

---

### 3. Benchmark/baseline datasets & testing strategy

- **Current limitation: synthetic data**  
  Today, many tests rely on **synthetic data** and code-only checks, which are limited in catching production-equivalence issues.

- **Idea: baseline datasets & outputs from DS**  
  We discussed asking DS to share **baseline input data + engineered feature outputs + (optionally) model outputs** to serve as benchmark datasets for validation.

- **Automated distribution comparison**  
  With those baselines, we can compare **distributions between OSDS and GCP** (e.g., for features and/or scoring outputs) to detect regressions in an automated way.

- **Applicability across models**  
  While the value depends on each model and data coverage, the general **benchmark approach** is reusable across multiple model migrations.

---

### 4. Post-scoring, consumption, and entitlements

- **Post-scoring tables and validation**  
  After model output is available, there is a **post-scoring pipeline** whose outputs must be validated first by engineering, then by DS.

- **Entitlements for Tableau and consumers**  
  To allow consumers (e.g., Tableau users) to access post-scoring data, we need to create an **entitlement / AD group** and grant read access to the relevant tables.

- **Ownership of consumption enablement**  
  Engineering is responsible for **getting post-scoring data ready and accessible** (tables, entitlements), while consumption logic and analysis sit with downstream teams.

---

### 5. Monitoring & audit tables

- **Model monitoring handled by a specialized team**  
  Long-term **model performance monitoring** (drift, performance degradation) is out of scope for this team and handled elsewhere.

- **Audit table for pipeline runs (engineering-owned)**  
  We plan to design a common **audit table** to log each run: job ID, input/output counts, duration, status, etc., so we can track pipeline executions over time.

- **Potential reporting & alerts from audit data**  
  Audit data can drive **basic reporting and alerts** (e.g., runtime spikes, volume anomalies) to help us detect operational issues early.

---

### 6. Release calendar and code-freeze considerations

- **Need for a clear release calendar**  
  We want a **shared release calendar** that lists planned releases (e.g., 9/21, 10/2, etc.), code-freeze windows (e.g., Diwali, local freezes), and fallback release options.

- **Handling issues in a given release**  
  Knowing the **next possible release date** helps us plan remediation if a particular release (e.g., 9/21) encounters issues.

- **Impact of regional code freezes**  
  We need to factor in **regional freezes** (such as around Diwali) so we can plan production activities and exceptions/approvals in advance.

---

### 7. Table types, external vs internal, and Dataplex

- **Scoring/post-scoring tables as external**  
  There is a likely requirement that **scoring and post-scoring tables** be defined as **external tables** so downstream consumers can access them correctly.

- **Dataplex lineage requirements**  
  Making tables external may require **Dataplex lineage entries / DRT IDs**, which introduces additional steps in the production release process.

- **Next release impact**  
  These changes (external tables + Dataplex) were deferred from the current release and are candidates for the **next production release cycle**.

---

### 8. CI/CD and automation improvements

- **Current CI/CD coverage**  
  We already have CI, PR practices, and some unit/integration tests, but much of the testing is still **manual and local**.

- **Potential use of comparison scripts (Gerardo’s work)**  
  There is an existing **CI script** that compares configuration/code changes between branches to detect unexpected diffs; we’d like a demo and to explore generalizing it.

- **Goal: more automated, repeatable checks**  
  The long-term goal is to incorporate **data-focused checks (distributions, configs, smoke tests)** into CI/CD so that validation is consistent across models.

---

### 9. Platform access and tools (Tachyon, notebooks)

- **Need for exploratory tools for validation**  
  To perform distribution checks and exploratory analysis, we need access to tools like **Jupyter notebooks** in a suitable environment.

- **Exploring Tachyon’s project space**  
  We will check if we can leverage the **Tachyon platform project** (with notebooks) for these validation tasks and what access is required.

---

### 10. UAT vs smoke/integration testing

- **“UAT” in current process**  
  The current “UAT” step is mostly **smoke and integration testing**, ensuring that pipelines run successfully and screenshots can be attached for change requests.

- **True UAT = DS + business validation**  
  True UAT, in this context, is when **DS and business users validate the outputs on real-like data** and sign off on the results as fit for production.

---

### 11. Jira structure and foundational stories

- **Separate Jira for release vs foundation**  
  One Jira (e.g., ZK‑572) is used purely for **current release work**, while three **foundational stories** (e.g., 518, 515, 523) cover longer-term common work.

- **Subtasks under foundational stories**  
  Items like logging, audit tables, benchmark testing, and CI improvements will be captured as **subtasks** under these foundational stories.

- **Staggered work across sprints**  
  Plan is to **pull one foundational story per sprint** and continue adding/refining subtasks as we clarify scope and priorities.

---

### 12. Documentation, validation checklist, and communication

- **Validation checklist & documentation**  
  We plan to create a **validation checklist + procedural documentation** to standardize how we test pipelines and models across different migrations.

- **Two planned email threads**  
  We will:
  - Start an email thread on **benchmark datasets and testing alignment** with DS.  
  - Start another on **common-utils backlog** (logging, audit tables, alerting, etc.) and ownership.

- **Follow-up meetings and demos**  
  We intend to schedule:
  - A session with DS and engineering to align on testing and benchmarks.  
  - A demo with **Gerardo** to walk through his CI comparison script and see how it can be adopted.

---

If I’ve missed anything important from the discussion, please let me know so we can refine this memo. Otherwise, we can use this as a reference when planning the next steps and backlog items.

Thanks,  
Jun

---

Would you like a shorter “management” version of this memo that keeps only the top 5–6 bullets?



Here are two draft emails based on your discussion. You can adjust names, project names, and technical details as needed.

---

## Email 1 – Align with Data Scientists on Baseline / Benchmark Datasets

**Subject:** Aligning on baseline datasets for model & pipeline validation on GCP

Hi **Jason**, **Anand**, **Shiva**,  

Cc: **Rukuya**, **[other relevant DS / Eng names]**

As we progress with the ACH model migration to GCP and start planning for additional models, I’d like to align with the data science team on how we can strengthen our validation approach beyond the current synthetic-data–based checks.

Right now, most of our automated validation is focused on:

- Code-level checks (CI, unit tests, integration tests), and  
- Limited runs using **synthetic data** to verify that pipelines execute end-to-end.

This helps catch basic issues, but it doesn’t fully validate that the **new GCP pipelines are consistent with the existing production behavior**.

### Proposal: Shared baseline / benchmark datasets

Would it be possible for the data science team to help define and (where allowed) share:

1. **Baseline input datasets**
   - A curated slice of historical data that is representative of real production traffic (e.g., a few days or weeks).
   - Enough coverage to exercise typical and edge-case scenarios the model cares about.

2. **Baseline *feature-engineering outputs***
   - The corresponding **feature tables** (after feature engineering) that data scientists currently trust and use for model development/validation.
   - This is especially important for us because our current checks mainly confirm that the code runs, not that the **feature values match what DS expects**.

3. **Optional: Baseline model outputs / score distributions**
   - If feasible, a baseline view of the **score distributions** or key metrics that DS use when they validate the model (e.g., for ACH).
   - This would allow us to build automated checks to compare GCP runs against known-good distributions (within reasonable tolerances).

### How we intend to use this

With access to such baseline datasets, we can:

- Compare **feature distributions** between:
  - Existing / legacy environment (e.g., OSDS)  
  - New GCP pipelines
- Build **automated regression checks** in CI/CD to detect:
  - Unexpected changes in feature distributions
  - Breaking changes in pipeline logic across releases
- Increase confidence that:
  - A successful technical run in GCP also means **business-equivalent behavior** to the current production environment.

Even partial or anonymized / sampled datasets would already be a big improvement over purely synthetic data. We understand there may be constraints around data sharing and access, so we’d appreciate guidance on what is feasible from your side.

### Concrete next steps

Could we:

1. Confirm whether such **baseline datasets (inputs + engineered features)** exist today for ACH (and other models), and  
2. Discuss what can be shared or exposed (e.g., via a controlled GCP project / notebooks / read-only tables) for the engineering team’s validation use?

If this sounds reasonable, I’m happy to:

- Draft a more concrete proposal (including how we’d integrate this into CI/CD), and  
- Set up a short working session with DS and engineering to finalize the approach.

Thanks,  
Jun  

---

## Email 2 – Common Utils, Logging, Audit Tables, and Testing Backlog

**Subject:** Consolidating common-utils, logging, and testing work items for model migration

Hi **Jason**, **Anand**, **Shiva**,  

Cc: **Rukuya**, **Prithvi**, **[others as needed]**

Following our recent discussions on the ACH migration and the broader model-migration foundational work, I’d like to summarize and align on the **common-utils and validation backlog** that we should plan for the upcoming sprints.

### 1. Common utils & observability

We’ve identified a few cross-cutting components that should ideally be implemented as reusable utilities rather than per-model custom code:

- **Centralized logging**
  - Standard log format and levels across pre-processing, scoring, and post-scoring jobs.
  - Clear correlation (e.g., job IDs, run IDs) to support troubleshooting and post-incident analysis.

- **Audit table**
  - A common audit schema to capture, per run:
    - Job ID / pipeline ID  
    - Input row counts / output row counts  
    - Start/end times and duration  
    - High-level status and error flags
  - This will support both operational monitoring and basic performance trend analysis (e.g., detection of sudden runtime spikes).

- **Alerting hooks**
  - Integration points so production/ops teams can be notified (e.g., on job failures, abnormal run times, or unexpected volume drops/spikes).
  - We’ve started some initial alerting work, but might need refinement after we see how it behaves in production.

These should ideally live under the **common-utils** work so that all migrated models can use a consistent framework.

### 2. Validation & testing framework

From the ACH discussion, we also have a growing list of validation-related items that span multiple models:

- **Feature-level validation**
  - Comparing distributions between the legacy environment (e.g., OSDS) and GCP for pre-processing outputs.
  - Potential integration with DS-provided **benchmark datasets** (as proposed in a separate email).

- **Post-scoring validation**
  - Similar comparison approach for post-scoring tables, plus DS sign-off on output reasonableness.

- **CI/CD integration**
  - Incorporate some of these checks (distribution comparisons, config sanity checks, etc.) into the CI pipelines.
  - There is prior work (e.g., Gerardo’s code comparison script) that we’d like to review and potentially generalize.

### 3. Platform and access questions

We also need alignment on some platform-level aspects:

- **Use of Tachyon’s project space / notebooks**
  - Whether we can leverage their environment (e.g., Jupyter notebooks) for running distribution comparisons and exploratory validation.

- **Table types and Dataplex**
  - For scoring and post-scoring tables, we need to clarify:
    - When they must be defined as **external tables** in GCP, and  
    - The **Dataplex lineage / DRT ID** requirements for production.
  - This likely affects how we structure the next release, especially for consumer access (e.g., Tableau).

### 4. Suggested next steps

To move this forward, I propose:

1. We use the existing **foundational Jira stories** as umbrellas and add subtasks for:
   - Logging framework  
   - Audit table design/implementation  
   - Alerting refinement  
   - Validation utilities (distribution comparison, etc.)

2. We have a short planning session (30–45 minutes) with DS and engineering to:
   - Prioritize which of these items are most critical before additional models go live on GCP.
   - Clarify ownership (engineering vs. DS vs. platform teams) for each area.
   - Confirm expectations for post-production issue handling and who is on point for different types of incidents.

Please let me know if this summary aligns with your understanding, and if you’re okay with us structuring the backlog this way. I’m happy to schedule a call and draft the initial subtasks in Jira based on this outline.

Thanks,  
Jun  

---

If you’d like, I can also shorten either email or tailor them to specific stakeholders (e.g., one version only for DS, another only for platform/infra).