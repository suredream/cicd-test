Wrap the 5 comparison phases (input → bronze/silver/gold → feature processing → scoring/Tachyon → post-scoring) into a single automated script chain, built on top of Gerardo's existing 160-story harness rather than starting from scratch.
Standardize each phase's output to a common format — status (pass/fail), diff summary, evidence link — so results are comparable across models even though the underlying data differs.
Designate one team member to run the script chain per model and have it write results directly into a shared tracking sheet (model, phase, status, evidence, owner, timestamp).
Let the tracking sheet be the single source of readiness — anyone on the team can check a model's status without asking in chat or sitting through a walkthrough.